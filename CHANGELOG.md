# تاریخچهٔ تغییرات — سامانه انبارداری سورین

## 1.7.12 — ۱۴۰۵/۰۶/۱۱

- **رفعِ باگِ بصریِ پنجرهٔ فیلترها** در «موجودی انبار» و «مدیریت انبار»: پس از
  اعمالِ فیلتر، سرستونِ چسبانِ جدول روی پنجرهٔ کشوییِ فیلترها می‌افتاد و بخشی از
  آن را می‌پوشاند. z-indexِ سرستونِ چسبان از ۲۰ به ۱۰ رسید تا زیرِ پنجرهٔ
  فیلترها/اکشن‌ها بماند (ولی همچنان بالای ردیف‌ها). این پنجرهٔ کشویی برای
  اکشن‌های ردیفی هم بود و آن‌جا هم اصلاح شد.


## 1.7.11 — ۱۴۰۵/۰۶/۱۱

- **انتقال به انبارِ دیگر** در «مدیریت انبار»: کنارِ حذف کالا، حالا می‌توان چند
  موجودی را تیک زد و یک‌جا (یا تک‌ردیفی) به انبارِ دیگر منتقل کرد. کلِ موجودیِ
  آزادِ هر سطر با حفظِ قیمتِ تمام‌شدهٔ لات (FIFO) جابه‌جا می‌شود و سندِ خروج/ورود
  در کاردکس می‌ماند — هیچ سندی حذف نمی‌شود.
- **نمایشِ انبارِ نگهداری** در «موجودی انبار» و «مدیریت انبار» وقتی فیلترِ انبار
  روی «همه» است: نامِ هر انبار با رنگی متمایز، تا محلِ نگهداریِ هر کالا یک‌نگاهه
  دیده شود (با فیلترِ یک انبارِ مشخص، این ستون تکراری است و پنهان می‌شود).
- **ردیف‌های یک‌درمیانِ سفید/خاکستری** در هر دو فهرست، تا هنگامِ خواندن، سطرِ هر
  کالا با سطرِ بالا و پایینش اشتباه گرفته نشود.


## 1.5.4 — ۱۴۰۵/۰۶/۰۳

- فوتر: نمایش سال تأسیسِ میلادی هم درست شد (بازهٔ «سال تأسیس – امسال» در همهٔ زبان‌ها).
- ماشین‌حساب پروژه: افزایش فاصلهٔ عمودیِ ردیف‌های جدول.
- صفحهٔ لایسنس: بولد‌کردن آدرس ولت در بخش خرید، و بلوک تلگرامِ آبی با لوگو در پایین صفحه.
- به‌روزرسانی اسکرین‌شات‌های لایسنس.


## 1.5.3 — ۱۴۰۵/۰۶/۰۳

- ماشین‌حساب پروژه: افزایش فاصلهٔ ستون‌های جدول (خواناتر).
- صفحهٔ لایسنس: آدرس ولت **بولد** شد و راه تماس تلگرام (t.me/Soorin_Support) به‌صورت لینک اضافه شد.
- افزودن قیمت، آدرس ولت و تلگرام به README (بخش خرید).


این فایل خلاصهٔ هر نسخه را نگه می‌دارد تا بدانیم هر آپدیت چه چیزی را اضافه یا
اصلاح کرده است. شمارهٔ نسخه از فایل `VERSION` در ریشهٔ پروژه خوانده می‌شود و در
فوتر سامانه و صفحهٔ «به‌روزرسانی» هم دیده می‌شود. با هر تغییرِ برنامه، نسخه
بالا می‌رود تا نصب‌های زنده بتوانند آپدیت را تشخیص دهند.

قالب تاریخ‌ها شمسی است.

---

## 1.5.2 — ۱۴۰۵/۰۶/۰۴

- **رفع محدودیت سال تأسیس** در شخصی‌سازی (اکنون سال میلادی هم پذیرفته می‌شود؛ بازهٔ ۱۰۰۰ تا ۲۲۰۰).
- **افزودن تلفن و آدرس** به پایین همهٔ گزارش‌های چاپی و انبارگردانی (اگر در شخصی‌سازی پر شده باشند).

## 1.5.1 — ۱۴۰۵/۰۶/۰۳

**سوییچ زبانِ دراپ‌داون + خروجی انگلیسیِ اسکریپت‌ها**

- انتخاب زبان از دکمه‌ها به **دراپ‌داون لیست** تغییر کرد (نوار بالا، صفحهٔ ورود، نصب).
- خروجی ترمینالِ اسکریپت‌های نصب و SSL (`debian-setup.sh`، `install-ssl-helper.sh`،
  `soorin-ssl`) کاملاً **انگلیسی** شد تا در ترمینال درست نمایش داده شود.
- دستور نصبِ دستیار SSL حالا مسیر پروژه را هم نشان می‌دهد
  (`cd /var/www/soorin-inventory`).

## 1.5.0 — ۱۴۰۵/۰۶/۰۳

**افزودن پنج زبان: روسی، چینی، آلمانی، فرانسوی، ایتالیایی**

- ترجمهٔ کامل هر پنج زبان برای همهٔ ۲۳ فایل زبان (شامل لایسنس و دسترسی‌ها).
- سوییچ زبان اکنون هشت زبان دارد: فارسی، English، العربية، Русский، 中文،
  Deutsch، Français، Italiano.
- تستِ برابری کلیدها همهٔ زبان‌ها را می‌سنجد (۱۱۰۵ assertion).

## 1.4.0 — ۱۴۰۵/۰۶/۰۳

**بخش «درباره و حمایت» + تنظیم پرداخت**

- بخش «درباره و حمایت» در صفحهٔ لایسنس: پیام سازنده، آدرس ولت تتر، «۶ ماه رایگان،
  سپس فقط ۵ تتر»، و دعوت به دونیت — به هر سه زبان.
- آدرس ولت تتر و شبکه (TRC20) و قیمت در تنظیمات لایسنس ثبت شد.
- پس از فعال‌سازیِ لایسنس، دیگر ساعت مانیتور نمی‌شود.

## 1.3.2 — ۱۴۰۵/۰۶/۰۳

**مهلت ۶ ماهه + ضدِ دستکاریِ ساعت در لایسنس**

- مهلتِ استفاده بدون لایسنس از ۱۴ روز به **۱۸۰ روز (۶ ماه)** تغییر کرد.
- تشخیص عقب‌کشیدنِ ساعت: برنامه بیشترین تاریخِ دیده‌شده را نگه می‌دارد؛ اگر کاربر
  ساعت سرور را عقب بکشد (بیش از ۲ روز رواداری) پنل قفل می‌شود، و محاسبهٔ مهلت/انقضا
  بر مبنای همان بیشینه است تا مهلت دور زده نشود. لایسنسِ دائمیِ معتبر مستثناست.
- راهنمای توزیع/فروش: جای‌گذاریِ کلید عمومی در config برای کلونِ گیت‌هاب.

## 1.3.1 — ۱۴۰۵/۰۶/۰۲

**قفل لایسنس بر اساس Hardware ID**

- به‌جای قفلِ دامنه، لایسنس به **شناسهٔ سخت‌افزارِ سرور (HWID)** قفل می‌شود
  (App\Support\Hardware از machine-id). HWID در صفحهٔ لایسنس نمایش داده می‌شود تا
  مشتری برای فروشنده بفرستد.
- ابزارِ صدورِ لایسنسِ فروشنده (جداگانه و خارج از مخزن) کلید را از روی HWID می‌سازد.
- به‌روزرسانی دستور `soorin:make-license` (‎--hwid) و ترجمه‌های fa/en/ar.

## 1.3.0 — ۱۴۰۵/۰۶/۰۲

**سیستم لایسنس برای فروش محصول**

- لایسنسِ آفلاین و امضاشده (Ed25519) — تأیید محلی بدون نیاز به اینترنت.
- صفحهٔ «لایسنس» در پنل: نمایش وضعیت، واردکردن کلید، و اطلاعات خرید شامل
  **آدرس ولت تتر (USDT)**، شبکه، قیمت و راه ارتباط با فروشنده.
- مدل: دائمی و قفل‌شده به یک دامنه/آی‌پی؛ دورهٔ آزمایشی ۱۴ روزه، سپس قفلِ پنل تا
  واردکردن کلید (بنر هشدار در دورهٔ آزمایشی).
- دستورهای فروشنده: `soorin:license-keys` (ساخت جفت‌کلید) و `soorin:make-license`
  (صدور کلید برای هر مشتری).

## 1.2.1 — ۱۴۰۵/۰۶/۰۲

**افزودن زبان عربی**

- زبان عربی (العربية) به‌طور کامل اضافه شد؛ همهٔ ۲۱ فایل زبان ترجمه شد.
- راست‌به‌چپ و ارقام عربی‌هندی (٠١٢٣) و تقویم میلادی برای عربی، خودکار.
- تستِ برابری کلیدها اکنون همهٔ زبان‌ها را می‌سنجد (fa/en/ar).

## 1.2.0 — ۱۴۰۵/۰۶/۰۲

**چندزبانه‌شدن سامانه (افزودن انگلیسی)**

- زبان انگلیسی به‌طور کامل اضافه شد؛ همه‌جا ترجمه شد: از نوار بالا تا فوتر،
  فرم‌ها، دسترسی‌ها، گزارش‌ها و پرینت‌ها، صفحه‌های خطا و صفحهٔ نصب.
- سوییچ زبان در نوار بالا و صفحهٔ ورود؛ انتخاب هر کاربر در پروفایلش ذخیره می‌شود.
- جهت صفحه (راست‌به‌چپ/چپ‌به‌راست)، تقویم (شمسی/میلادی) و شکل ارقام (فارسی/لاتین)
  خودکار با زبان عوض می‌شوند.
- دستورالعمل افزودن زبان‌های بعدی: `docs/TRANSLATIONS.md` (بدون کدنویسی).
- توجه: دادهٔ واردشده (نام کالا، مشتری، ارز و…) ترجمه نمی‌شود؛ فقط متنِ رابط.

## 1.1.2 — ۱۴۰۵/۰۶/۰۲

**اتصال واحد پول به بخش «ارز»**

- قیمت‌گذاری قطعه (ورود کالا، قیمت ورژن) دیگر به سه ارز ثابت محدود نیست؛ واحدها
  از بخش «ارز» خوانده می‌شوند. مثلاً اگر «روبل» را در «ارز» اضافه کنی، در ورود
  کالا، داشبورد، گزارش‌ها و ماشین‌حساب پروژه هم می‌آید.
- ریال/دلار/یوان به‌عنوان پیش‌فرض همیشه موجودند؛ ارزهای تازه روی این‌ها اضافه
  می‌شوند و اگر نامشان در جدول عوض شود، همان نمایش داده می‌شود.
- ریال به جدول ارز افزوده شد (نصب‌های موجود با مهاجرت به‌روز می‌شوند).

## 1.1.1 — ۱۴۰۵/۰۶/۰۲

**تکمیل قابلیت SSL**

- افزودن اسکریپت نصبِ دستیار SSL (`deploy/install-ssl-helper.sh`) که در نسخهٔ
  قبل جا مانده بود؛ حالا نصبِ خودکار و راهنمای داخل پنل کامل کار می‌کند.

## 1.1.0 — ۱۴۰۵/۰۶/۰۲

**قابلیت SSL / HTTPS در پنل**

- بخش تازهٔ «SSL» برای مدیر با دو حالت:
  - **self-signed** برای سرور داخلی (HTTPS فعال می‌شود؛ هشدار یک‌بارهٔ مرورگر).
  - **Let's Encrypt** برای سرور عمومی با دامنه، با **تمدید خودکار** (certbot).
- گزینهٔ **«اجبار HTTPS»** (ریدایرکت خودکار http→https) و «برداشتن SSL».
- توضیحات کامل فارسی در صفحه تا کاربرِ غیرفنی راحت انتخاب کند.
- کارهای ریشه‌ای از راه یک دستیارِ root با قانون sudoers محدود انجام می‌شود (خودِ
  برنامه root نمی‌شود)؛ پیش از هر تغییر nginx تست و در صورت خطا بازگردانی می‌شود.

## 1.0.9 — ۱۴۰۵/۰۶/۰۲

**سخت‌سازی امنیتی (بعد از بازبینی کامل)**

- نصب‌کنندهٔ وب: پاک‌سازی خط جدید در مقادیر `.env` (دفاع در عمق برابر تزریق تنظیمات).
- قالب Nginx دبیان: افزودن سرآیندهای امنیتی (`X-Content-Type-Options`،
  `X-Frame-Options`، `Referrer-Policy`) و یک CSP روی پوشهٔ لوگوها تا SVGِ آلودهٔ
  احتمالی هنگام باز شدن مستقیم، اسکریپت اجرا نکند.

## 1.0.8 — ۱۴۰۵/۰۶/۰۱

**«اتصال به گیت‌هاب» برای نصب‌های زیپ**

- اگر برنامه با فایل زیپ نصب شده باشد (پوشهٔ `.git` ندارد)، حالا می‌توان با یک
  دکمه در صفحهٔ «به‌روزرسانی» یا دستور `php artisan soorin:link-github` آن را به
  گیت‌هاب وصل کرد تا از آن پس «به‌روزرسانی از گیت‌هاب» هم کار کند.
- فایل‌های برنامه با نسخهٔ مخزن هم‌تراز می‌شوند؛ `.env`، دیتابیس و لوگوهای آپلودی
  دست‌نخورده می‌مانند و پیش از کار پشتیبان گرفته می‌شود.

## 1.0.7 — ۱۴۰۵/۰۶/۰۱

**رفع مشکل «به‌روزرسانی از گیت‌هاب» روی سرور دبیان**

- روی دبیان، PHP-FPM با محیطِ پاک‌شده اجرا می‌شود؛ به همین دلیل آپدیت از داخل
  مرورگر می‌توانست شکست بخورد (composer پیدا نمی‌شد یا HOME تنظیم نبود). حالا
  محیطِ سالمی (PATH/HOME/COMPOSER_HOME) به دستورهای git و composer داده می‌شود.
- به‌روزرسانی راهنمای نصب دبیان: توضیح حالت مخزن public (بدون توکن) و اینکه فقط
  نصب با git clone امکان به‌روزرسانی از گیت‌هاب را می‌دهد.

## 1.0.6 — ۱۴۰۵/۰۶/۰۱

**افزودن صفحهٔ «شخصی‌سازی» (تغییر نام کسب‌وکار و لوگو توسط مدیر)**

- صفحهٔ تازه در مسیر **مدیریت و گزارش ← شخصی‌سازی** (فقط برای مدیر).
- مدیر می‌تواند این‌ها را عوض کند:
  - نام کسب‌وکار (فارسی و انگلیسی) و عنوان سامانه
  - آدرس وب‌سایت، متن نمایشی وب‌سایت و سال تأسیس
  - تلفن و نشانی (برای سربرگ فاکتور و گزارش‌های چاپی)
  - لوگوها: لوگوی روشن، لوگوی تم شب، نشان مربعی و فاوآیکون
- این مقادیر همه‌جا اعمال می‌شوند: نوار بالا، فوتر و کپی‌رایت، فاکتور و
  گزارش‌های چاپی، صفحه‌های خطا و آیکون زبانهٔ مرورگر.
- پیش‌نمایش زنده از نوار بالا و فوتر، و دکمهٔ «بازگردانی به پیش‌فرض».
- لوگوهای آپلودی در `public/branding` ذخیره می‌شوند و سرِ آپدیت پاک نمی‌شوند
  (وابسته به `storage:link` هم نیست).

## 1.0.5 — ۱۴۰۵/۰۶/۰۱

**رفع باگ‌های صفحهٔ دسترسی‌ها**

- رفع مشکلی که با زدن یک تیک، تیک دیگری خودبه‌خود می‌خورد یا برداشتن یک تیک،
  همهٔ تیک‌ها را پاک می‌کرد و ذخیره خطا می‌داد.
- هر گروه دسترسی حالا یک فیلد مستقل است، پس تیک‌ها روی هم اثر نمی‌گذارند.

## 1.0.4 — ۱۴۰۵/۰۵/۳۱

**افزودن ماشین‌حساب پروژه**

- در بخش «سامانه و پروژه»: انتخاب چند مدل سامانه با تعداد، و محاسبهٔ قطعات
  موردنیاز، قیمت کل به تفکیک ارز (ریال/دلار/یوان)، و وضعیت موجودی هر قطعه.
- محاسبهٔ قیمت مستقل از موجودی است.

## 1.0.3 — ۱۴۰۵/۰۵/۲۹

**اصلاح پیام خطای به‌روزرسانی**

- وقتی دریافت از گیت‌هاب ناموفق باشد، پیام خطای واقعی نمایش داده می‌شود.

## 1.0.2 — ۱۴۰۵/۰۵/۲۹

**سینک شمارهٔ نسخه**

- شمارهٔ نسخهٔ فوتر با صفحهٔ «به‌روزرسانی» یکسان شد (هر دو از فایل `VERSION`).

## 1.0.1 — ۱۴۰۵/۰۵/۲۹

**به‌روزرسانی درون‌برنامه‌ای و مستندات نصب**

- قابلیت آپدیت از داخل پنل: هم آفلاین (فایل زیپ) و هم آنلاین (گیت‌هاب)، با
  بررسی نسخه و گرفتن پشتیبان پیش از آپدیت.
- نمایش شمارهٔ نسخه در فوتر.
- نصب‌کنندهٔ وب خودکار (شبیه وردپرس) و راهنمای نصب روی دبیان (زیپ + گیت‌هاب).

## 1.0.0 — ۱۴۰۵/۰۵/۲۶

**نخستین نسخهٔ کامل**

- مدیریت کالا و دسته‌بندی، انبارها، موجودی و ثبت ورود/خروج.
- انبارگردانی با ورودِ درجا، تفکیک «پایان» از «به‌روزرسانی انبار» و امکان لغو.
- خرید و واردات، مشتریان، پروژه‌ها و مدل‌های سامانه با تعریف قطعات.
- قیمت واحد هر قطعه به تفکیک ارز، داشبورد ارزش موجودی، شماره سریال.
- گزارش‌های چاپی (پیش‌نمایش چاپ به‌جای PDF) با ستون ردیف، سرستون چسبان،
  سورت و فیلتر.
- دسترسی تیک‌خور مستقیم روی هر کاربر (مدیر می‌تواند دسترسی را بگیرد).
- پشتیبان‌گیری و بازیابی دیتابیس، تم روز/شب و چیدمان موبایلی.
