# قوانینِ کار روی سامانه انبارداری سورین

> راهنمای کاملِ پروژه در `.claude/PROJECT_GUIDE.md` است؛ در ابتدای هر جلسه آن و
> `docs/PROJECT_STATE.md` را بخوان. این فایل خلاصهٔ قوانینِ حیاتی است که نباید فراموش شوند.

## انتشارِ نسخه — بعد از هر تغییرِ برنامه (اجباری، بدون پرسیدنِ دوباره)

هر تغییری که روی نصبِ زنده باید دیده شود، باید این چرخه را کامل طی کند. معیارِ
«نسخهٔ جدید» در برنامه فقط فایلِ `VERSION` است، نه کامیت‌ها؛ بدونِ بالابردنِ آن،
دکمهٔ «بررسی به‌روزرسانی» چیزی پیدا نمی‌کند.

1. **فایلِ `VERSION` را یک پله بالا ببر** — `patch` برای رفعِ باگ/تغییرِ کوچک،
   `minor` برای قابلیتِ تازه (semver).
2. **یک بخش در بالای `CHANGELOG.md`** با نسخهٔ جدید و تاریخِ شمسیِ امروز اضافه کن
   (سبکِ فارسیِ بخش‌های قبلی).
3. **کامیت و پوش روی `main`:** `git push origin main` — سرورِ زنده آپدیت را از
   `main` می‌کشد.
4. **ریلیزِ گیت‌هاب بساز:** `gh release create vX.Y.Z --title "vX.Y.Z" --target main --notes "…"`
   با توضیحِ مختصرِ فارسیِ چه‌چیزی اضافه/اصلاح شد (هماهنگ با ریلیزهای قبلی).

## قواعدی که نباید بشکنی (خلاصه — کاملش در PROJECT_GUIDE)

- موجودی و قیمت روی `item_versions`، نه `items`.
- `system_versions` (نقشه) از `customer_systems` (اجراشده) جداست.
- هیچ رکورد `stock_movements` حذف/ویرایش نمی‌شود؛ اصلاح فقط با سند معکوس.
- همه‌جا SoftDelete؛ داده تاریخی حذف نمی‌شود.
- هیچ اتصالی به سامانهٔ پشتیبانی (`Soorin_Support`) ساخته نشود.
- متنِ رابط فارسی و در `lang/fa/`، نه هاردکد در Blade. رنگ‌ها فقط از
  `config/branding.php`/متغیرهای CSS.
- نام فایل/کلاس دقیقاً PSR-4 (سرور لینوکس است؛ حساس به بزرگی/کوچکیِ حروف).
